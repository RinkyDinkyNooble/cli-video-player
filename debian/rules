#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	# Nothing to build — pure Python script

override_dh_auto_install:
	install -D -m 0755 debian/asciiv.wrapper debian/asciiv/usr/bin/asciiv
	install -D -m 0644 main.py debian/asciiv/usr/lib/asciiv/main.py
